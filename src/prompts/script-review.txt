You are the editor of the hypnosis script below. Judge it as one whole session rather than as separate sections: does it read as a single continuous arc, spoken in one voice, from induction through to the end?

Judge every "##" section against these questions:

1. Continuity — does it follow on from the state the previous section left the listener in? A listener already deep is not re-inducted, already-established anchors are not introduced twice, and the intensity never resets.
2. Escalation — does it move the arc forward, or does it restate what the section before it already did?
3. Repetition — does it reuse phrasing, imagery or suggestions another section has already used?
4. Setups and payoffs — are the triggers, anchors and suggestions planted earlier actually carried through and resolved by the end?
5. Brief coverage — taken together, do the sections deliver what the brief asked for?

The brief was:

{brief}

Length (measured, not estimated — treat these numbers as fact):

{lengthBrief}

You are MARKING this script, not rewriting it. Record what you find and stop there: do not write, revise or lengthen a single section. A reader reads your findings and decides what to act on, and a finding they can disagree with is worth more than a rewrite they never saw.

Record the review by calling `critique_record` once, with stage "review":

* Use verdict "pass", with no findings, when every section earns its place in the arc.
* Use verdict "revise" and one finding per section at fault. Name a section at most once, and say in one line what is wrong with it — which of the five questions above it fails, and how.
* Quote the passages that carry the fault in `spans`. Each span must be copied CHARACTER FOR CHARACTER out of that section as it reads below — not paraphrased, not tidied, not summarised — long enough to appear exactly once in that section. A span that is too short, that cannot be found, or that appears more than once is refused with the fault named, and you must quote again. Where the fault is the section as a whole rather than any passage in it, record the finding with no spans and say so in the reason.
* Cite no rule numbers: this pass judges the arc, not the numbered style rules.

Flag only real problems: a section that genuinely earns its place in the arc is not a finding.

If no tools are available to you, reply instead with one line per "##" section, in script order, in one of these two formats and nothing else — no preamble, no commentary, no code fences:

VERDICT: <section title> | cohesive
VERDICT: <section title> | revise | <one line naming the specific problem>

Here is the script to review:

{script}
